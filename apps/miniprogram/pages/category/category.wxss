.type-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.type-card {
  position: relative;
  display: flex;
  justify-content: space-between;
  gap: 14rpx;
  min-height: 150rpx;
  padding: 22rpx;
  overflow: hidden;
  border: 2rpx solid #deddd2;
  border-radius: 18rpx;
  background: #fff;
  box-shadow: 0 10rpx 30rpx rgba(32, 36, 31, 0.06);
}

.type-card view {
  min-width: 0;
}

.type-mark {
  position: absolute;
  top: 0;
  left: 0;
  width: 12rpx;
  height: 100%;
  background: #b55c3b;
}

.type-card-live .type-mark {
  background: #b55c3b;
}

.type-card-static .type-mark {
  background: #2f4b3f;
}

.type-card-mobile .type-mark {
  background: #e0a922;
}

.type-card-desktop .type-mark {
  background: #20241f;
}

.type-card view text:first-child {
  display: block;
  overflow: hidden;
  color: #20241f;
  font-size: 31rpx;
  font-weight: 820;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.type-card view text:last-child {
  display: block;
  margin-top: 12rpx;
  overflow: hidden;
  color: #77766e;
  font-size: 23rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.type-card-live {
  border-color: rgba(216, 90, 58, 0.42);
}

.type-card-live view text:first-child {
  color: #b55c3b;
}

.type-card-static {
  border-color: rgba(47, 75, 63, 0.38);
}

.type-card-static view text:first-child {
  color: #2f4b3f;
}

.type-card-mobile {
  border-color: rgba(232, 181, 67, 0.56);
}

.type-card-mobile view text:first-child {
  color: #b37a12;
}

.type-count {
  align-self: flex-end;
  flex-shrink: 0;
  max-width: 112rpx;
  overflow: hidden;
  color: #20241f;
  font-size: 24rpx;
  font-weight: 780;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.type-card-desktop {
  border-color: rgba(32, 36, 31, 0.32);
}

.type-card-other {
  border-color: rgba(119, 129, 122, 0.34);
}

.type-card-other .type-mark {
  background: #77817a;
}

.category-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18rpx;
  margin-top: 26rpx;
}

.category-card {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: flex-end;
  min-height: 172rpx;
  padding: 22rpx;
  overflow: hidden;
  border-radius: 18rpx;
  color: #fff;
  background:
    linear-gradient(145deg, rgba(32, 36, 31, 0.1), rgba(32, 36, 31, 0.78)),
    linear-gradient(135deg, #2f4b3f, #9b6a42);
  font-size: 32rpx;
  font-weight: 800;
}

.category-card text {
  min-width: 0;
}

.category-card::before {
  position: absolute;
  top: 18rpx;
  left: 18rpx;
  width: 52rpx;
  height: 52rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.24);
  border-radius: 16rpx;
  content: "";
}

.category-index {
  position: absolute;
  top: 24rpx;
  left: 24rpx;
  color: rgba(255, 255, 255, 0.72);
  font-size: 20rpx;
  font-weight: 760;
}

.category-name {
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.category-count {
  margin-top: 10rpx;
  color: rgba(255, 255, 255, 0.74);
  font-size: 22rpx;
  font-weight: 620;
}

.category-card:nth-child(3n + 2) {
  background:
    linear-gradient(145deg, rgba(32, 36, 31, 0.08), rgba(32, 36, 31, 0.72)),
    linear-gradient(135deg, #6f745f, #b99a49);
}

.category-card:nth-child(3n) {
  background:
    linear-gradient(145deg, rgba(32, 36, 31, 0.08), rgba(32, 36, 31, 0.78)),
    linear-gradient(135deg, #20241f, #4f604a);
}

.category-skeleton-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18rpx;
  margin-top: 26rpx;
}

.category-skeleton-card {
  height: 172rpx;
  border-radius: 10rpx;
  background:
    linear-gradient(90deg, rgba(255, 255, 255, 0), rgba(255, 255, 255, 0.78), rgba(255, 255, 255, 0)),
    #e7ece9;
  background-size: 240rpx 100%, 100% 100%;
  animation: tagSkeletonSweep 1.4s ease-in-out infinite;
}

@keyframes tagSkeletonSweep {
  0% {
    background-position: -260rpx 0, 0 0;
  }
  100% {
    background-position: 420rpx 0, 0 0;
  }
}
